import React from "react";
import SearchBar from "../searchBar/SearchBar";
import "./Hero.css";

const Hero = () => {
  return (
    <div className="hero-container">
      <SearchBar />
    </div>
  );
};

export default Hero;
